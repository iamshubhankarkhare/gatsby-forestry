---
label: HeroBlock2
hide_body: true
fields:
- name: sections
  type: blocks
  template_types:
  - text
  config:
    min: 
    max: 
  label: Sections
  description: All different sections inside heroblock.
