---
label: HeroBlock2
hide_body: true
fields:
- name: sections
  type: blocks
  template_types:
  - text
  config:
    min: 
    max: 
  label: Sections
  description: All different sections inside heroblock.
- name: type
  type: number
  label: Type
  description: Type of Hero Block.
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
