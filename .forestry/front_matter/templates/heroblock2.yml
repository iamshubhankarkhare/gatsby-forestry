---
label: HeroBlock2
hide_body: true
fields:
- name: heading
  type: blocks
  label: Heading
  template_types:
  - text
  config:
    min: 
    max: 
  description: 'Choose different sections: title,subtitle,description, media etc.'
- name: cta
  type: include
  config: {}
  template: cta
  label: CTA
  description: A list of call to actions
- name: type
  type: number
  description: Type of hero block
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
  label: Type
